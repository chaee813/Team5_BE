namespace: default
resources:
  - mysql.yaml
  - backend.yaml
  - ingress.yaml
  - nginx.yaml
configMapGenerator:
  - name: nginx
    files:
      - configs/default.conf
  - name: init-db
    files:
      - configs/init.sql
secretGenerator:
- name: database-creds
  files:
  - username.txt
  - password.txt
